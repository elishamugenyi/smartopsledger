/** Departments: organizations this account may own (create). */
export const MAX_OWNED_ORGANIZATIONS = 2;

/** Max users with MEMBER role per organization (ADMIN does not count). */
export const MAX_MEMBER_ROLE_PER_ORGANIZATION = 2;
